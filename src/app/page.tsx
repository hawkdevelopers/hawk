import Hero from "@/components/main/Hero";
import React from "react";

const page = () => {
  return (
    <main style={{
      display: "flex"
    }}>
      <Hero />
    </main>
  );
};

export default page;
